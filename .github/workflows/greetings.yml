name: Greetings

on:
  issue_comment:
    types: [created]

permissions:
  issues: write
  pull-requests: write

jobs:
  greeting:
    name: Comment Greeting
    runs-on: ubuntu-latest
    if: github.event.comment.user.login == github.event.issue.user.login
    steps:
      - name: No-op placeholder for future contributor-recognition automation
        run: echo "Reserved for future community-recognition automation."
